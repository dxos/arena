#
# App-specific constants and defaults.
#

app:
  name: 'Arena'
  rootElement: 'root'

devtools: true

client:
  keyStorage:
    root: "dxos/apps/keys"

  feedStorage:
    root: "dxos/apps/feeds"

  swarm:
    signal: "wss://demo.kube.dxos.network/dxos/signal"
    ice:
      - urls: "stun:stun.wirelne.ninja:3478"
      - urls: "turn:stun.wireline.ninja:3478"
        username: "wireline"
        credential: "wireline"

services:
  wns:
    server: "https://node1.dxos.network/wns/api"
    chainId: "wireline"

debug:
    mode: 'development'
