#
# App-specific constants and defaults.
#

app:
  name: 'Arena'
  rootElement: 'root'

devtools: true

client:
  keyStorage:
    root: "dxos/arena/keys"

  feedStorage:
    root: "dxos/arena/feeds"
    type: "idb"

  swarm:
    signal: "wss://demo.kube.dxos.network/dxos/signal"
    ice:
      - urls: "stun:stun.wirelne.ninja:3478"
      - urls: "turn:stun.wireline.ninja:3478"
        username: "wireline"
        credential: "wireline"

debug:
    mode: 'development'
