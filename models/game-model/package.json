{
  "name": "@dxos/game-model",
  "version": "1.0.0",
  "main": "dist/src/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:protobuf && tsc --build",
    "build:protobuf": "mkdir -p src/proto/gen && build-protobuf src/proto/defs/game.proto -o src/proto/gen"
  },
  "dependencies": {
    "@dxos/codec-protobuf": "^0.2.0",
    "@dxos/model-factory": "^2.5.2-alpha.0",
    "@dxos/util": "^2.1.0",
    "debug": "^4.2.0"
  },
  "devDependencies": {
    "@dxos/protobuf-compiler": "^0.2.0",
    "typescript": "^4.0.3"
  },
  "private": "true"
}
